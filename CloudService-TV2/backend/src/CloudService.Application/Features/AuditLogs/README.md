# Audit logs — Owner: Trưởng nhóm

`IAuditLogService` cung cấp truy vấn paging/filter chỉ dành cho Admin. Auth ghi các sự kiện bảo mật qua abstraction `IAuthStore`; response không trả credential, token hoặc secret.
